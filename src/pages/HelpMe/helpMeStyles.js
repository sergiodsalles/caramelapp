import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    helpMeContainer: {
        padding: 15,
    },
    helpTypePicker: {
        fontSize: 16,
    },
    itemStyle: {
        fontSize: 16,
        padding: 20,
    }

});